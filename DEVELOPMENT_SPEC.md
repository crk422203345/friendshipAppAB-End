# A/B/C 多端 App 基础模板开发规范

## 1. 模板定位

本仓库是 A 端、B 端、C 端共同使用的 uni-app Vue 3 App 模板，不承载任一端的具体业务。

- A、B、C 均以 Android/iOS App 为主要发布目标。
- 微信小程序保留为兼容构建目标，不主导目录、性能或功能设计。
- 每个端应从本模板创建独立应用工程，并独立维护包名、签名、图标、推送、支付和商店资料。

## 2. 共享与隔离边界

### 可以沉淀到模板

- `core/`：网络请求、会话、存储、错误模型。
- `services/`：上传、运行时信息、权限适配接口、升级和推送的接入点。
- `config/`：A/B/C 构建档与默认环境配置。
- `components/`：无业务语义、跨端复用的基础组件。
- `styles/`、`uni.scss`：全局设计令牌和基础样式。

### 必须留在端侧工程

- 首页、TabBar、业务页面、角色规则与接口权限。
- Android 包名、iOS Bundle ID、签名证书、应用图标和启动图。
- 推送厂商参数、支付商户号、地图/IM/音视频等 SDK 密钥。
- 隐私政策、用户协议、注销流程及应用市场素材。

禁止将 A/B/C 任一端的业务页面、接口地址、测试账号或密钥回写到模板默认配置。

## 3. 目录约定

```text
config/       A/B/C 构建档和环境入口
core/         请求、会话、存储等无 UI 依赖能力
services/     上传、权限、推送、升级等应用服务
platform/     仅在需要原生能力时新增 Android/iOS 适配实现
components/   轻量通用组件
pages/        模板页或具体端工程的页面
static/       模板 Logo 和必要的轻量资源
```

业务功能进入端侧工程的 `modules/` 或 `pages/`，不要在模板中预建聊天、支付、会员等空壳模块。

## 4. 构建档

`config/app-profiles.js` 定义 `template`、`a`、`b`、`c` 四个构建档。

```bash
npm run dev:app:a
npm run build:app:b
npm run build:app:c
```

构建档只用于选择默认应用配置和服务地址。原生包名、证书、推送和支付密钥不能通过前端构建档替代，必须在各端独立工程的 `manifest.json` 与安全配置中维护。

## 5. App 开发规则

- 业务页面使用 Vue 3 `<script setup>`。
- uView Pro 使用 `pages.json` 的 easycom 自动按需编译，禁止手动全量注册组件。
- App 权限遵循最小化原则：只在功能真正使用时申请相机、相册、定位、通知等权限。
- 任何 IM、音视频、地图、支付 SDK 都不得在 `main.js` 全局初始化，应由具体业务模块按需加载。
- 上传、推送、版本更新等服务必须通过 `services/` 封装，页面不直接依赖平台 API。
- `manifest.json` 中不得保留不使用的 Android 高风险权限。
- 每个端发布前必须在真机验证启动、登录、权限拒绝、弱网、后台恢复、升级和注销流程。

## 6. 小程序兼容规则

- 小程序页面和 App 页面可共享业务逻辑，但原生能力必须通过条件编译或服务适配隔离。
- 小程序有包体积限制时，再在端侧工程配置 `subPackages`、资源 CDN 和预下载规则。
- 不以小程序分包规则限制 App 的模块目录；App 默认整包，启动优化按实际性能数据决定。

## 7. 发布检查

每个端在发布前至少完成：

1. 配置独立应用名称、包名、Bundle ID、图标、版本号与签名。
2. 将生产 API、推送、支付、监控等密钥放入安全的端侧发布配置。
3. 执行对应构建命令，并在 Android/iOS 真机回归。
4. 检查 Android/iOS 权限声明与隐私政策是否一致。
5. 检查崩溃上报、网络异常、登录失效和账号注销流程。
